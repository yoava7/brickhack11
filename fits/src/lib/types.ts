export interface ClothingItem {
    id: string;
    name: string;
    image_url: string;
    type: string;  // e.g. "shirt", "pants"
    color: string;
    material: string;
    pattern: string;
  }
  